import { Injectable } from '@nestjs/common'; import { PrismaService } from '../prisma/prisma.service'; import { UserStatus } from '@prisma/client'; @Injectable() export class AdminService{constructor(private prisma:PrismaService){} async dashboard(){const [users,lost,found,reunited,closed,recent]=await this.prisma.$transaction([this.prisma.user.count(),this.prisma.animal.count({where:{situation:'LOST'}}),this.prisma.animal.count({where:{situation:'FOUND'}}),this.prisma.animal.count({where:{situation:'REUNITED'}}),this.prisma.animal.count({where:{situation:'CLOSED'}}),this.prisma.animal.count({where:{createdAt:{gte:new Date(Date.now()-86400000)}}})]);return {users,lost,found,reunited,closed,newLast24h:recent}}
 users(){return this.prisma.user.findMany({select:{id:true,name:true,email:true,city:true,state:true,role:true,status:true,createdAt:true,_count:{select:{animals:true}}},orderBy:{createdAt:'desc'}})}
 animals(){return this.prisma.animal.findMany({include:{owner:{select:{name:true,email:true}},_count:{select:{sightings:true}}},orderBy:{createdAt:'desc'}})}
 async userStatus(id:string,status:UserStatus,actorId:string){const user=await this.prisma.user.update({where:{id},data:{status}});await this.prisma.auditLog.create({data:{actorId,action:'USER_STATUS_CHANGED',entityType:'User',entityId:id,metadata:{status}}});return {id:user.id,status:user.status}}
 logs(){return this.prisma.auditLog.findMany({include:{actor:{select:{name:true,email:true}}},orderBy:{createdAt:'desc'},take:100})}}
