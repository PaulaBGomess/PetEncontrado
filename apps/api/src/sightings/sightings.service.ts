import { Injectable,NotFoundException } from '@nestjs/common'; import { PrismaService } from '../prisma/prisma.service'; import { CreateSightingDto } from './dto'; @Injectable() export class SightingsService{constructor(private prisma:PrismaService){} async create(animalId:string,userId:string,dto:CreateSightingDto,file?:Express.Multer.File){if(!await this.prisma.animal.findUnique({where:{id:animalId}}))throw new NotFoundException('Animal não encontrado');return this.prisma.sighting.create({data:{animalId,reporterId:userId,sightingDate:new Date(dto.sightingDate),approximateTime:dto.approximateTime,street:dto.street,neighborhood:dto.neighborhood,city:dto.city,state:dto.state.toUpperCase(),referencePoint:dto.referencePoint,description:dto.description,photoUrl:file?`/uploads/${file.filename}`:undefined}})} async mine(userId:string){return this.prisma.sighting.findMany({where:{reporterId:userId},include:{animal:{select:{id:true,name:true,species:true,city:true,situation:true}}},orderBy:{createdAt:'desc'}})} async received(userId:string){return this.prisma.sighting.findMany({where:{animal:{ownerId:userId}},include:{animal:{select:{id:true,name:true,species:true,city:true,situation:true}},reporter:{select:{name:true,email:true,whatsapp:true}}},orderBy:{createdAt:'desc'}})}}
