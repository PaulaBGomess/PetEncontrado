import { Injectable,NotFoundException } from '@nestjs/common'; import { PrismaService } from '../prisma/prisma.service'; import { UpdateProfileDto } from './dto'; @Injectable() export class UsersService{constructor(private prisma:PrismaService){} async me(id:string){const user=await this.prisma.user.findUnique({where:{id},select:{id:true,name:true,email:true,phone:true,whatsapp:true,city:true,state:true,role:true,status:true,createdAt:true}});if(!user)throw new NotFoundException();return user} update(id:string,dto:UpdateProfileDto){return this.prisma.user.update({where:{id},data:{...dto,state:dto.state?.toUpperCase()},select:{id:true,name:true,email:true,phone:true,whatsapp:true,city:true,state:true,role:true}})}}
