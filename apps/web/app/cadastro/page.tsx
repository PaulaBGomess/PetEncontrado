"use client";import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';import {Header} from '@/components/Header';import {useAuth} from '@/components/AuthProvider';export default function Cadastro(){const{register}=useAuth();const router=useRouter();const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const f=new FormData(e.currentTarget);const p=String(f.get('password')),c=String(f.get('confirm'));if(p!==c){setError('As senhas não coincidem');setLoading(false);return}try{await register(Object.fromEntries([...f.entries()].filter(([k])=>k!=='confirm')));router.push('/cadastrar-animal')}catch(e:any){setError(e.message)}finally{setLoading(false)}}return <><Header/><div className="form-shell"><form className="panel" onSubmit={submit}><h1 className="form-title">Criar conta</h1><p className="form-sub">Cadastre-se para publicar ocorrências e colaborar com avistamentos.</p>{error&&<div className="error">{error}</div>}<div className="form-grid"><div className="field full"><label>Nome completo</label><input name="name" required minLength={3}/></div><div className="field full"><label>E-mail</label><input name="email" type="email" required/></div><div className="field"><label>Telefone</label><input name="phone"/></div><div className="field"><label>WhatsApp</label><input name="whatsapp"/></div><div className="field"><label>Cidade</label><input name="city"/></div><div className="field"><label>Estado</label><input name="state" maxLength={2}/></div><div className="field"><label>Senha forte</label><input name="password" type="password" required minLength={8}/></div><div className="field"><label>Confirmar senha</label><input name="confirm" type="password" required/></div></div><p className="form-sub">Use no mínimo 8 caracteres, com maiúscula, minúscula, número e símbolo.</p><button className="btn primary" disabled={loading}>{loading?'Criando...':'Criar conta'}</button></form></div></>}
