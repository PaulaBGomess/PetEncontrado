"use client";import {FormEvent,useState} from 'react';import {useSearchParams} from 'next/navigation';import Link from 'next/link';import {Header} from '@/components/Header';import {apiFetch} from '@/lib/api';export default function Reset(){const p=useSearchParams();const[msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const a=String(f.get('password')),b=String(f.get('confirm'));if(a!==b){setMsg('As senhas não coincidem.');return}try{const r=await apiFetch('/auth/reset-password',{method:'POST',body:JSON.stringify({token:f.get('token'),password:a})});setMsg(r.message)}catch(e:any){setMsg(e.message)}}return <><Header/><div className="form-shell"><form className="panel" onSubmit={submit}><h1 className="form-title">Redefinir senha</h1>{msg&&<div className="success">{msg}</div>}<div className="form-grid"><div className="field full"><label>Token</label><input name="token" defaultValue={p.get('token')||''} required/></div><div className="field"><label>Nova senha</label><input name="password" type="password" minLength={8} required/></div><div className="field"><label>Confirmar</label><input name="confirm" type="password" required/></div></div><button className="btn primary">Alterar senha</button><p><Link href="/login">Ir para login</Link></p></form></div></>}
