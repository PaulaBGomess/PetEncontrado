"use client";import {FormEvent,useState} from 'react';import Link from 'next/link';import {Header} from '@/components/Header';import {apiFetch} from '@/lib/api';export default function Forgot(){const[msg,setMsg]=useState('');const[token,setToken]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{const r=await apiFetch('/auth/forgot-password',{method:'POST',body:JSON.stringify({email:f.get('email')})});setMsg(r.message);if(r.resetToken)setToken(r.resetToken)}catch(e:any){setMsg(e.message)}}return <><Header/><div className="form-shell"><form className="panel" onSubmit={submit}><h1 className="form-title">Recuperar senha</h1><p className="form-sub">Informe seu e-mail. Em produção, o token deve ser enviado por um serviço de e-mail.</p>{msg&&<div className="success">{msg}</div>}<div className="field"><label>E-mail</label><input name="email" type="email" required/></div><div className="actions"><button className="btn primary">Gerar recuperação</button><Link className="btn" href="/login">Voltar</Link></div>{token&&<div className="panel" style={{marginTop:18}}><b>Token de desenvolvimento</b><p style={{wordBreak:'break-all'}}>{token}</p><Link href={`/redefinir-senha?token=${token}`}>Abrir redefinição</Link></div>}</form></div></>}
