"use client";import Link from 'next/link';import {useRouter} from 'next/navigation';import {FormEvent,useState} from 'react';import {Header} from '@/components/Header';import {useAuth} from '@/components/AuthProvider';export default function Login(){const{login}=useAuth();const router=useRouter();const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const f=new FormData(e.currentTarget);try{const authenticatedUser=await login(String(f.get('email')),String(f.get('password')));router.replace(authenticatedUser.role==='ADMIN'?'/admin':'/meus-anuncios')}catch(e:any){setError(e.message)}finally{setLoading(false)}}return <><Header/><div className="form-shell"><form className="panel" onSubmit={submit}><h1 className="form-title">Entrar</h1><p className="form-sub">Acesse sua conta para gerenciar anúncios e avistamentos.</p>{error&&<div className="error">{error}</div>}<div className="form-grid"><div className="field full"><label>E-mail</label><input name="email" type="email" required autoComplete="email"/></div><div className="field full"><label>Senha</label><input name="password" type="password" required autoComplete="current-password"/></div></div><button className="btn primary" disabled={loading}>{loading?'Entrando...':'Entrar'}</button><p className="form-sub" style={{marginTop:16}}><Link href="/esqueci-senha">Esqueci minha senha</Link> · Ainda não possui conta? <Link href="/cadastro">Criar conta</Link></p></form></div></>}
