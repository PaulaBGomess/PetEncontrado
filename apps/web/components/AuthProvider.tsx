"use client"; import {createContext,useContext,useEffect,useMemo,useState} from 'react';import {apiFetch} from '@/lib/api';
type User={id:string;name:string;email:string;role:'USER'|'ADMIN'};type Ctx={user:User|null;token:string|null;loading:boolean;login:(e:string,p:string)=>Promise<User>;register:(d:any)=>Promise<void>;logout:()=>Promise<void>;refresh:()=>Promise<boolean>};const C=createContext<Ctx|null>(null);
export function AuthProvider({children}:{children:React.ReactNode}){const[user,setUser]=useState<User|null>(null);const[token,setToken]=useState<string|null>(null);const[loading,setLoading]=useState(true);async function refresh(){try{const r=await apiFetch('/auth/refresh',{method:'POST'});setToken(r.accessToken);setUser(r.user);return true}catch{setToken(null);setUser(null);return false}}useEffect(()=>{refresh().finally(()=>setLoading(false))},[]);async function login(email:string,password:string):Promise<User>{const r=await apiFetch('/auth/login',{method:'POST',body:JSON.stringify({email,password})});setToken(r.accessToken);setUser(r.user);return r.user as User}async function register(d:any){const r=await apiFetch('/auth/register',{method:'POST',body:JSON.stringify(d)});setToken(r.accessToken);setUser(r.user)}async function logout(){await apiFetch('/auth/logout',{method:'POST'}).catch(()=>{});setToken(null);setUser(null)}const v=useMemo(()=>({user,token,loading,login,register,logout,refresh}),[user,token,loading]);return <C.Provider value={v}>{children}</C.Provider>}export function useAuth(){const c=useContext(C);if(!c)throw new Error('useAuth fora do provider');return c}
