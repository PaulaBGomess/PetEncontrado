import Link from 'next/link';import {CalendarDays,MapPin} from 'lucide-react';import {imageUrl} from '@/lib/api';export function AnimalCard({a}:{a:any}){const label:any={LOST:'Perdido',FOUND:'Encontrado',REUNITED:'Reunido',CLOSED:'Encerrado'};return <article className="animal-card"><div className="animal-photo">{/* eslint-disable-next-line @next/next/no-img-element */}<img src={imageUrl(a.images?.[0]?.url)} alt={a.name||a.species}/><span className={`status ${a.situation.toLowerCase()}`}>{label[a.situation]}</span></div><div className="animal-body"><div className="eyebrow">{a.species} {a.breed?`• ${a.breed}`:''}</div><h3>{a.name||`${a.species} encontrado`}</h3><p className="meta"><MapPin size={16}/>{a.neighborhood} • {a.city}/{a.state}</p><p className="meta"><CalendarDays size={16}/>{new Date(a.occurrenceDate).toLocaleDateString('pt-BR')}</p><Link className="card-link" href={`/animais/${a.id}`}>Ver detalhes →</Link></div></article>}
